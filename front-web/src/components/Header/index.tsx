import './styles.css';

const Header = () => {
  return (
    <header className="main-header-container">
      <h1 className="header-title">DS Sales</h1>
    </header>
  );
};

export default Header;
